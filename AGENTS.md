# 仓库级 AI 工作规范

本文件位于仓库根目录，是本仓库的全局提示词，适用于根目录及其全部子目录。任何 AI 在处理本仓库中的任务前，都必须先完整阅读并遵守本文件；如工作目录到仓库根目录之间还存在更具体的 `AGENTS.md`，应一并阅读并遵守。

系统指令、开发者指令和用户在当前任务中的明确要求优先级更高。下级目录的 `AGENTS.md` 可以增加或细化规则，但不得在没有明确说明的情况下忽略本文件。

## 每次任务开始前

AI 必须按顺序完成以下检查：

1. 阅读仓库根目录的 `AGENTS.md`，以及当前工作目录适用的所有下级 `AGENTS.md`。
2. 运行 `git status`，识别当前分支、用户已有改动和未跟踪文件。
3. 阅读与任务相关的目录、代码、文档和配置，确认现有实现及约束。
4. 明确本次任务的修改范围、验证方式以及可能影响的文件。

不得跳过上述检查后直接修改文件。

## 沟通语言

- 默认使用简体中文与用户沟通，包括过程说明、完成报告和问题描述。
- 新增或修改文档时默认使用简体中文，除非文件既有语言、技术规范或用户要求使用其他语言。
- Git 提交信息必须使用简体中文，不使用无意义的英文描述，也不要在提交信息中提及 AI。
- 代码标识符、命令、协议字段和第三方专有名词可保留英文。

## 工作原则

- 修改前先了解目录结构、现有实现和相关约束，不根据文件名盲目判断。
- 开始工作前检查 `git status`，区分用户已有改动和本次任务改动。
- 只修改完成当前任务所必需的文件，不顺手重构无关代码。
- 优先沿用仓库已有的架构、命名、格式和工具链。
- 不删除、不覆盖、不回退用户已有改动；遇到同一文件中的已有改动时，应保留并在其基础上工作。
- 不提交密钥、令牌、密码、个人数据、构建产物或其他不应进入版本库的内容。
- 不执行 `git reset --hard`、强制推送等破坏性操作，除非用户明确要求并确认风险。

## 验证要求

- 修改完成后运行与改动相关的测试、检查或构建命令。
- 无法运行验证时，应明确说明原因，不得声称已经通过。
- 提交前再次检查差异，确认没有混入无关文件、调试内容或敏感信息。

## 自动 Git 提交与远程推送

- 每个任务完成并通过必要验证后，自动创建一个本地 Git commit，无需再次询问用户。
- 只暂存并提交本次任务产生的文件或代码块，不得把任务开始前就存在的无关改动一起提交。
- 如果无法可靠分离本次改动与用户已有改动，应暂停自动提交并向用户说明原因。
- 没有实际文件变化时不创建空提交。
- 用户明确要求暂不提交、只分析或只给方案时，不创建提交。
- 验证失败且问题尚未解决时，不得把任务描述为已完成；是否保留未提交改动应在结果中说明。
- 不修改或合并已有提交，不使用 `git commit --amend`，除非用户明确要求。
- 本地提交成功后，默认立即将当前分支推送到远程仓库，无需再次询问用户。
- 默认使用当前分支已经配置的 upstream；如果尚未配置 upstream，但存在 `origin`，使用 `git push -u origin <当前分支>` 建立关联并推送。
- 用户明确要求不推送、仅保留本地提交或离线工作时，不执行远程推送。
- 没有实际文件变化或没有创建新提交时，不执行无意义的推送。
- 推送前确认目标远程和目标分支，不向其他分支推送，也不顺带推送标签。
- 如果远程包含本地没有的提交，禁止强制推送或擅自改写历史；应停止推送并向用户说明需要同步的情况。
- 如果因网络、认证、权限、分支保护或远程配置问题导致推送失败，应保留本地提交，并在最终回复中说明失败原因和当前提交哈希。
- 不使用 `git push --force` 或 `git push --force-with-lease`，除非用户明确要求并确认风险。

提交信息使用以下中文格式：

```text
<类型>：<简明摘要>
```

推荐类型：`功能`、`修复`、`重构`、`文档`、`测试`、`构建`、`维护`。

示例：

```text
文档：整理提示词目录并补充使用说明
修复：处理空配置导致的启动失败
重构：拆分用户查询与响应组装逻辑
```

## 完成标准

任务只有在以下条件满足后才算完成：

1. 用户要求已经落实，而不只是给出建议。
2. 已完成适合当前改动的验证，或明确说明无法验证的原因。
3. 已检查最终差异和 Git 状态。
4. 已按本文件规则创建本地 Git commit，并完成远程推送；如未提交或未推送，已明确说明合理原因。
5. 最终回复简要列出主要改动、验证结果、提交哈希、提交信息和远程推送结果。
